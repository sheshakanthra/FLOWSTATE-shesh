# PROGRESS

Claude Code reads this at the start of every session and updates it at the end. Keep it short — it is a state file, not a journal.

## Current session

**Next up:** C1 — Copilot dock & context envelope
**Spec:** `docs/phases/C1-copilot-dock-context.md`

## Sessions

| ID | Session | Status | Gate passed |
|----|---------|--------|-------------|
| A1 | Repo scaffold & token pipeline | done | yes |
| A2 | Form & display primitives | done | yes |
| A3 | Overlay primitives | done | yes |
| A4 | Motion, Shimmer, EmberEdge, FiringBar, undo | done | yes (item 1's 60fps claim needs a manual Performance-panel look — see Known issues) |
| A5 | Data primitives & layout primitives | done | yes |
| A6 | App shell, workspace, seed data | done | yes |
| B1 | Agent canvas foundation | done | yes (item 1's fps figure is an automated proxy, not a human Performance-panel read — see Known issues) |
| B2 | Node registry & typed connections | done | yes (item 8's fps figure is an environment artifact this session, not a real regression — see Known issues) |
| B3 | Inspector, autosave, undo integration | done | yes (item 2's network-failure retry path is verified via a hook-level test and manual browser QA, not a scripted e2e — see Deferred) |
| B4 | Run engine & test console | done | yes — a real, latent RLS bug (present since A6, misdiagnosed as "environment flakiness" in B3) was found and fixed this session; see Decisions and the corrected Known issues entry |
| B5 | Trace replay | done | yes — all 9 gate items verified against 3 real recorded runs (succeeded/failed/cancelled) from B4's own live Groq testing, not synthetic fixtures; see Decisions |
| B6 | Versioning, publish, templates | done | yes — all 8 gate items verified against the real running app (real Postgres trigger, real Playwright-driven publish flow, real Groq runs for all 6 templates, real 500-agent perf query); see Decisions |
| C1 | Copilot dock & context envelope | not started | — |
| C2 | Streaming, messages, citations | not started | — |
| C3 | Tool registry, previews, approval | not started | — |
| D1 | Priority queue & ranking | not started | — |
| D2 | In-flight zone & live updates | not started | — |
| E1 | Marketing hero live demo | not started | — |

## Decisions

Record only decisions that are NOT obvious from reading the code. Format: `[session] decision — reason`.

- `[A1]` Font variable classNames (`switzer.variable`, `commitMono.variable`) are applied on `<html>`, not `<body>`. `--font-sans`/`--font-mono` are declared at `:root` inside `@theme` and reference `--font-switzer`/`--font-commit-mono`; a custom property's `var()` references are resolved once, where the property is declared, using values visible at that element — not re-resolved per consumer down the tree. Putting the font classes on `<body>` left `--font-switzer` invisible at `:root`, so `--font-sans` computed to guaranteed-invalid (empty) everywhere. Fixed by moving the classNames up to `<html>`.
- `[A1]` The `no-color-literals` ESLint rule only flags bare CSS named colors (e.g. `"red"`, `"blue"`) when the string is the value of a color-ish property/attribute (`color`, `background`, `fill`, `border*Color`, etc.) — not everywhere. `red` and `blue` are both valid CSS named colors *and* this codebase's semantic token names, so a blanket ban would break ordinary variant props like `variant="red"`. Hex and `rgb()`/`hsl()` literals are still caught unconditionally everywhere, matching the spec's "must fail CI" requirement without false-positiving on the vocabulary the design system itself uses.
- `[A1]` Semantic triples (`{color}-fg`/`-bg`/`-line`) get distinct, explicitly-computed hex/rgba values per theme rather than a live `color-mix()` formula. `-fg` is darkened per hue for light-mode AA contrast (the dark-mode hues are too light to read as text on a near-white background); `-bg`/`-line` are rgba tints of that same darkened hue. This satisfies the gate's "toggling theme changes every swatch" requirement for all fifteen semantic tokens, not just the neutrals.
- `[A1]` No `clsx`/`tailwind-merge` dependency — `lib/utils.ts` has a small dependency-free `cn()` instead, since neither package is in CLAUDE.md's approved dependency table.
- `[A1]` Switzer and Commit Mono were downloaded fresh (Fontshare / commitmono.com, both free-for-commercial-use) and subset to Latin + a small punctuation/symbol range with `fonttools` before being committed to `public/fonts/` as woff2. Only the four Switzer weights and Commit Mono 400 named in the spec were kept.
- `[A2]` Storybook pinned at `8.6.18` (latest 8.x patch) — plain `storybook@latest`/`npx storybook init` now resolves to Storybook 10, so every `@storybook/*` package and `storybook` itself is pinned explicitly rather than range-versioned.
- `[A2]` `.storybook/main.ts` sets `process.env.__NEXT_PRIVATE_RENDER_WORKER = "1"` before the config object. Next 15.4.6's config loader overrides Node's webpack module resolution to force its own compiled bundle, which crashes `@storybook/builder-webpack5` during `Compiler.close()` ("Cannot read properties of undefined (reading 'tap')") on every build. This env var is the same fix Storybook shipped upstream for this exact crash (storybookjs/storybook#32313) — without it, `pnpm build-storybook` and `pnpm storybook` both fail on this Next patch version.
- `[A2]` No Radix primitive exists for Combobox. `components/ui/combobox.tsx` is hand-built on `@radix-ui/react-popover` (non-modal, `onOpenAutoFocus` prevented so typing keeps focus) plus a manual WAI-ARIA combobox/listbox pattern (`role="combobox"`, `aria-activedescendant`, arrow-key/Enter/Escape handling).
- `[A2]` `--fg-200` and `--fg-300` (both themes) fail WCAG AA 4.5:1 against every `--ink-000`..`--ink-300` surface at normal 12–14px text — confirmed by computing contrast ratios, not just axe's say-so (fg-200 vs ink-100 is *worse* than vs ink-000, so it's not a "wrong surface tier" issue). Real copy that must be legible (Card/FormField descriptions, Select group labels, Combobox empty-state, Tabs inactive label, Select's rendered placeholder text) uses `fg-100` instead. `fg-200`/`fg-300` are still used, but only for decorative icons and for text inside elements with the native `disabled` attribute (which axe and WCAG 1.4.3 both exempt from contrast checks).
- `[A2]` The shared `focusRing` helper (`lib/utils.ts`) toggles `outline-color` only (`outline-transparent` → `focus-visible:outline-blue-fg`), never `outline-style`/`outline-none`. Tailwind v4's `outline-none` sets the shared `--tw-outline-style` custom property to `none`; because that property isn't scoped to a pseudo-class, `focus-visible:outline` (which just reads the property) stays `none` forever regardless of focus state, so the classic v3-era `outline-none focus-visible:outline` pattern silently renders no ring at all under v4. Verified with a computed-style check across six components after the fix.
- `[A2]` `@radix-ui/react-select` has no `modal` prop (unlike Popover/Dialog): opening it always `aria-hide`s everything outside its own subtree, including the trigger, when the app has a single DOM root (Storybook's `#storybook-root`, same shape as the real app's `#__next`). This trips axe's `aria-hidden-focus` rule but is inherent Radix behavior, not a defect — `FocusScope` traps the real tab order so the hidden trigger is never actually reachable. Scoped off via a per-story `parameters.a11y.config` override on `select--open` only, not disabled globally.
- `[A2]` `shadow-card` / `shadow-floating` / `blur-floating` (elevation formulas from `docs/DESIGN.md`) were added to `app/tokens.css` and its `@theme` block rather than written inline in components, since the ESLint color rule (and the tokens.css file header) intend raw color/rgba literals to live only in that one file.
- `[A2]` `Button` primary = `bg-ink-400` / `border-ink-500` / `text-fg-000`. DESIGN.md says primary is "near-white on graphite, no accent" without pinning exact tokens; `ink-400` is literally "near-white text on graphite" in dark (the source theme), and the light-theme rendering is a mechanical derivation of the same token pair, not a hand-tuned override.
- `[A3]` **Sheet vs Drawer** (user decision, spec didn't define the split): `Drawer` is the modal variant — built on `@radix-ui/react-dialog`, has a scrim, traps focus, blocks the page, slides from any of 4 edges, exits the way it entered. `Sheet` shares the identical slide/edge/motion code but is non-modal (`modal={false}` fixed, not exposed as a prop) — no scrim, no focus trap, rest of the page stays interactive. Gate items 1–3 (focus trap, scroll lock) apply to Dialog/Drawer/CommandPalette/DropdownMenu/ContextMenu (Radix's inherently-modal primitives), not to Sheet/HoverCard/Tooltip/default Popover.
- `[A3]` **Reduced motion** (user decision): added a minimal `useReducedMotion()` hook to `lib/motion.ts` — `matchMedia("(prefers-reduced-motion: reduce)")` read via `useSyncExternalStore`, called directly by every A3 overlay. This is deliberately *not* the root-context version CLAUDE.md/DESIGN.md describe ("read once at the root, provided by context") — that's A4's named scope. The hook's zero-arg/boolean-return signature is stable so A4 can swap its internals for a context read without touching call sites in dialog.tsx etc.
- `[A3]` **Shared open-state pattern**: every modal-family Root (Dialog/Drawer/Sheet/Popover/DropdownMenu/ContextMenu) is a small wrapper function around the Radix `Root`, not a direct `export const X = XPrimitive.Root` re-export like Select/Tabs use. It resolves controlled/uncontrolled `open` via a new shared hook (`lib/use-open-state.ts`, deliberately its own file rather than added to `lib/utils.ts` — `lib/utils.ts` is imported by the Server Component `card.tsx` for `cn` alone, and a hook forces `"use client"`), feeds the resolved boolean back into Radix's own `open`/`onOpenChange`, and provides it downward through a file-local `React.createContext` so `Content` can gate its own `<AnimatePresence>{open && <motion.div forceMount>}</AnimatePresence>` — Radix has no public API for a sibling part to read Root's resolved open state otherwise. This is a deliberate, one-time deviation from the established "no-styling parts are const re-exports" convention, not drift.
- `[A3]` Enter/exit animation goes through `motion/react` (the `motion` npm package, v11 — first real consumer in the codebase) driven by `lib/motion.ts`'s `fast`/`base` tokens, combined with Radix's `forceMount` prop on Portal/Content so the exit transition can play before React actually unmounts the node. `lib/motion.ts` already exports a const literally named `motion`; every overlay file imports the animation component as `motion` from `"motion/react"` instead and never imports the `lib/motion` bag by that name (only the individual tokens `fast`/`base`/`instant`/`useReducedMotion`).
- `[A3]` Toast stack (gate item 7, "6 simultaneous toasts without overflowing the viewport") is capped at `TOAST_LIMIT = 5` with FIFO eviction of the oldest, rather than a scrollable viewport — a scrollable stack would collide with Radix Toast's own swipe-to-dismiss gesture on touch devices. Verified via an automated story (`toast.stories.tsx`'s `Stack`) that clicks the trigger 6 times and asserts the DOM never holds more than 5, waiting past the evicted toast's exit animation before counting.
- `[A3]` CommandPalette is fully self-contained (manages its own `open` state, including the `⌘K`/`Ctrl+K` `document` keydown listener) since there's no app shell yet to host that wiring — A6 will most likely lift control via the `open`/`onOpenChange` props it already accepts, without touching this file. Listener binds both `metaKey` and `ctrlKey` for `"k"` so it's usable on this Windows dev machine, not just Mac.
- `[A3]` No `cmdk`, no `zustand` — CommandPalette's registry (`registerCommands`/`useCommands`) and Toast's queue are both a plain module-level store (`Map`/array + subscriber `Set`) exposed to React via `useSyncExternalStore`, since neither dependency was named by the spec and CLAUDE.md says not to add one that isn't.
- `[A3]` CommandPalette's motion tier is `base` (240ms) — not explicitly named in the spec's `fast`/`base` component lists, but grouped with Dialog by both radius (`xl`) and modal semantics, so `base` is the consistent inference rather than a guess left undocumented.
- `[A3]` `TooltipProvider`'s `delayDuration` is left to the caller to set (Radix's own default is 700ms) rather than hardcoded inside `tooltip.tsx` — 700ms reads sluggish for a dense builder UI, but that's a product judgment call for whichever session wires up the real Tooltip usages, not something to bake into the primitive.
- `[A4]` `useReducedMotion()`'s internals were swapped from a per-component matchMedia subscription to a context read, exactly as the A3 decision above flagged. `MotionContext` lives in `lib/motion.ts` (so every existing `@/lib/motion` import path stays valid — zero call-site changes in dialog/popover/toast/drawer/sheet/hover-card/tooltip/dropdown-menu/context-menu/command-palette); the actual `<MotionProvider>` component (the one thing that still touches `matchMedia`, once, via `useSyncExternalStore`) lives in `components/motion/motion-provider.tsx` per this session's file layout. Mounted in `app/layout.tsx` around `{children}` and in `.storybook/preview.tsx`'s decorator — skipping either would silently freeze every A3 overlay's reduced-motion behavior at the context default (`false`).
- `[A4]` Added a "Motion" Storybook toolbar global (`no-preference` / `reduce`) alongside the existing Theme/Density ones, and gave `MotionProvider` an optional `forceReducedMotion` prop the preview decorator passes through. Real app usage (`app/layout.tsx`) never passes it, so production always reflects the actual OS/browser preference — the override only exists so reduced-motion behavior can be exercised in Storybook without emulating the media feature at the OS level.
- `[A4]` Added two new primitives to `app/tokens.css`: `--ai-glow` (Shimmer/EmberEdge's achromatic luminance color) and `--gradient-spectral` (SpectralHairline's one gradient, built from the five existing semantic base hues rather than new literals). `--ai-glow` is theme-scoped like every other color in the file — dark mode brightens toward white, light mode darkens toward ink, since a "white delta" is invisible against a near-white surface. Same achromatic intent (luminance only, zero saturation) in both directions.
- `[A4]` Shimmer is a uniform whole-surface opacity breathing (0 → 8% → 0 of `--ai-glow`), not a moving gradient band — the spec's own notes call out that a moving band reads as a loading skeleton, which is exactly the effect it's meant to avoid.
- `[A4]` `SpectralHairline`'s duplicate-detection is a module-level mount counter (effect-based: increments on mount, decrements on unmount, warns via `console.warn` in dev when count > 1), not a React-context subtree boundary. The spec says "once per subtree," but nothing in this session names a boundary/provider component, and catching the most natural test case — two instances rendered as direct siblings — needs a counter shared across the whole commit, not just nested descendants a context `Provider` would catch. DESIGN.md's own framing ("**the single** spectral hairline") reads as a true singleton signal, so treating "more than one mounted at once, anywhere" as the violation condition matches that intent closer than a narrower subtree scope would.
- `[A4]` `FiringBar` segments are equal-width shares (`100 / count`%), not sized by any per-operation weight — `FiringOperation` has no "expected size" concept, and "proportionally sized" most plausibly reads as "proportional to how many are running," matching DESIGN.md's "a still screenshot shows three things running at once." Resize/enter/exit go through framer-motion's `layout` prop (transform-based, not a manual width recalculation) specifically for the 12-concurrent-segments-without-layout-thrash gate item.
- `[A4]` Gate item 4 asks for something stricter for the Firing Bar than the general reduced-motion rule: segments must appear/disappear "without animation," not with the usual 90ms crossfade every other component falls back to. `components/firing-bar/index.tsx` has a local zero-duration transition for exactly this case rather than reusing `lib/motion.ts`'s `instant` token.
- `[A4]` The Firing Bar's hover panel also opens on focus (a visually-hidden button, `sr-only focus:not-sr-only`, inside the same container) so keyboard users reach the same live-operations list a mouse-hover would show — the 3px strip itself is `aria-hidden` and can't hold focus. That button intentionally has **no** `onClick` toggle: clicking it fires a native `focus` event before the `click` handler runs, so `onFocus` already opens the panel by the time a click toggle would fire — a toggle immediately closes what focus just opened, so it never opens on click at all. Found via a headless Playwright run of the story's own `play` function failing, not by inspection.
- `[A4]` `useUndoable()` registers an already-applied mutation's inverse — it doesn't run the mutation itself. The spec's "every mutation is optimistic with a rollback, and registers an inverse with `useUndoable`" reads as: the mutation happens wherever it naturally does (a click handler, a TanStack Query `onMutate`), and this hook is purely the registration + confirmation-toast + ⌘Z-binding-guarantee step. `useUndoShortcuts()` (ref-counted, so N mounted `useUndoable()` callers still bind exactly one `document` keydown listener) is called unconditionally inside `useUndoable()`, so any component using the substrate gets the global shortcut for free.
- `[A4]` `.storybook/main.ts`'s `stories` glob was `../components/ui/**/*.stories.@(ts|tsx)` only — too narrow the moment a session (this one) puts stories outside `components/ui` (`components/motion`, `components/firing-bar`, `lib/undo`). Broadened to `../components/**/*.stories.@(ts|tsx)` and added `../lib/**/*.stories.@(ts|tsx)`, matching this session's own file layout instead of hardcoding paths for a fixed set of folders.
- `[A5]` `@tanstack/react-table` was pinned to `^8` explicitly (`8.21.3`) — a bare `pnpm add @tanstack/react-table` resolves to `9.0.0` (major, unreleased-stable-API-wise at spec-writing time), and the spec names "TanStack Table v8" specifically. `@tanstack/react-virtual` (same major family) and `@radix-ui/react-accordion` were also added — the former is spec-named ("+ virtualization"), the latter fits the existing "Radix UI, we wrap it ourselves" stack row rather than being a new library category.
- `[A5]` `DataTable`'s click/⌘-click/shift-range selection is layered on top of TanStack's own `rowSelection` state, not its default click handler (TanStack doesn't provide multi-select mouse semantics at all). Convention chosen: checkbox clicks are always additive (toggle without clearing, shift-click extends the range into the existing selection); row-body clicks are exclusive by default (Finder-style — plain click replaces the selection with just that row, ⌘/Ctrl-click toggles additively, shift-click replaces the selection with the contiguous range). Selection handlers reach the live sorted/filtered row model through a `tableRef` (mutated during render, read inside `useCallback`s) rather than a `rows` variable, to avoid a circular dependency: the injected `__select` column's `cell` needs the click handler, the handler needs the sorted row model, and the row model comes from `useReactTable(...)` — which needs the columns.
- `[A5]` `DataTable` keyboard nav (`j`/`k`/`Space`/`Enter`/`⌘A`) uses `aria-activedescendant` on the grid container plus a plain `activeIndex` number, not real DOM focus moved row-to-row — moving literal focus onto virtualized rows that mount/unmount as you scroll is fragile (the target row may not be mounted yet when you'd call `.focus()`). `⌘A` selects every row in the current sorted/filtered model (`table.getRowModel().rows`), not just the rows currently rendered in the viewport — "select all visible" reads as "all filtered results," the standard mail-client/spreadsheet meaning, not "all rows currently painted on screen."
- `[A5]` `Accordion`'s open/close height animation is plain CSS (`@keyframes` in `globals.css`, driven by Radix's own `--radix-accordion-content-height` custom property), not `motion/react` like every A3 overlay. Framer Motion's `height: "auto"` support is a measurement approximation; Radix ships this CSS var specifically so `AccordionContent`'s real open/closed transition — and Radix's own `Presence` unmount timing, which listens for the CSS animation to end — doesn't need JS measurement at all. Duration/easing (240ms, `cubic-bezier(0.32, 0.72, 0, 1)`) are hand-copied from `lib/motion.ts`'s `base` token to match the existing `transition-instant`/`transition-fast` CSS-mirror precedent in `globals.css`, since a CSS `@keyframes` block can't `import` a JS token.
- `[A5]` `TreeView` is hand-built (no Radix tree primitive exists, same reasoning as A2's Combobox): a WAI-ARIA `tree` with roving tabindex over a *flattened* list of currently-visible nodes (expanded-and-visible only), so Up/Down/typeahead/Home/End are a simple linear walk regardless of nesting depth, and Left/Right expand, collapse, or move to parent/first-child.
- `[A5]` `ResizablePanel` and `SplitPane` share one hook (`lib/use-resizable.ts`) for drag/keyboard/persistence rather than duplicating the logic — they differ only in layout (one edge-anchored panel vs. two panes sharing a divider). Persisted size lives in `localStorage` under `kiln:resizable:{id}`, written on every committed size change (drag end is continuous, so every pointermove commit persists — acceptable since it's a synchronous `setItem` on a tiny key, not a network write) and read once per panel `id` in a mount effect (a one-frame flash from `defaultSize` to the persisted value on load is an accepted tradeoff, same class as any localStorage-hydrated UI state).
- `[A5]` Design review (per the spec's Notes) was done as a throwaway Storybook story combining `PageHeader` + `DataTable` + `Card` + `Dialog` + `FiringBar`, screenshotted headlessly in both themes/densities, then deleted — it isn't a deliverable and isn't in the spec's file list. No token changes came out of it: density, hairlines, and type hierarchy all held up at 14px.
- `[A6]` **Contrast fix** (requested alongside this session's spec): nudged three semantic triples in `app/tokens.css` so every `-fg` clears 4.5:1 against every `--ink-000`..`--ink-300` surface in its theme — dark `--red-fg` `#e5484d` → `#e85a5e` (was failing on `ink-200`/`ink-300`), light `--emerald-fg` `#1c8f57` → `#177849` (was failing on *every* surface, including its own `ink-000` canvas), light `--amber-fg` `#96660b` → `#8f610a` (was failing on `ink-050`). `-bg`/`-line` were re-derived from the same new RGB so the triple stays internally consistent. Verified by computing actual WCAG contrast ratios (Node script), not by eyeballing — dark blue/violet and light red/blue/violet already cleared 4.5:1 everywhere and were left untouched. The now-unnecessary `color-contrast` a11y overrides in `dialog.stories.tsx`, `context-menu.stories.tsx`, `dropdown-menu.stories.tsx`, `data-table.stories.tsx`, and `bulk-action-bar.stories.tsx` were removed; `tag.stories.tsx`'s override is unrelated (WCAG's native-`disabled` exemption) and stays.
- `[A6]` **Postgres role split — required for RLS to mean anything.** Docker's `POSTGRES_USER` env var bootstraps that role as a Postgres *superuser*, and superusers bypass row-level security unconditionally — `FORCE ROW LEVEL SECURITY` has no effect on them (it only overrides the lesser *table-owner* exemption). Discovered by writing the RLS proof test first and watching it fail (cross-workspace row was still visible) before ever manually confirming the policy worked. Fix: a second role, `kiln_app`, created by hand in the running container (`CREATE ROLE kiln_app LOGIN PASSWORD ...; GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES ...; ALTER DEFAULT PRIVILEGES ... GRANT ... TO kiln_app`) — not a superuser, not the tables' owner, so RLS applies to it with no exceptions. `DATABASE_URL` (used by `db/client.ts`, i.e. every repo, the seed script, and both test suites) points at `kiln_app`; a separate `DATABASE_MIGRATION_URL` points at the owner role `kiln` and is used only by `db/migrate.ts` and `drizzle.config.ts`, since DDL (`CREATE TABLE`, `CREATE POLICY`, `ALTER TABLE ... FORCE ROW LEVEL SECURITY`) needs owner privileges `kiln_app` deliberately doesn't have. **A fresh non-Docker Postgres (or any managed Postgres) needs this same two-role setup by hand** — there's no migration for role creation since roles aren't database objects `drizzle-kit` manages, and a bootstrap superuser will exist under some name on most local setups even outside Docker.
- `[A6]` **`members` RLS policy has an `OR`, every other table's doesn't.** `lib/repos/workspaces.ts`'s `listWorkspacesForUser()` has to discover which workspaces a user belongs to *before* any single `workspace_id` is known — the standard multi-tenant RLS bootstrap problem. `members`' policy (`db/migrations/0001_row_level_security.sql`) is `USING (workspace_id = current_setting('app.workspace_id', true)::uuid OR user_id = current_setting('app.user_id', true)::uuid)`, so a membership row is visible either in the current workspace context or when it's the querying user's own row. `WITH CHECK` stays `workspace_id`-only (no `user_id` branch) so a user still can't write a membership row for a workspace they're not scoped into just by knowing their own id. `lib/repos/db.ts`'s `withScope()` sets `app.user_id` and/or `app.workspace_id` (either or both, whichever the caller has) as parameterized `set_config(..., true)` calls inside the same transaction as the query, never string-interpolated.
- `[A6]` **Session is a stateless HMAC-SHA256-signed cookie** (`lib/auth/session.ts`) carrying only `userId` + expiry — no `sessions` table, matching the schema list the spec actually named. Role is *not* in the cookie: it's resolved fresh from `[workspace]` + the DB on every navigation (`getWorkspaceContext`), so a role change or workspace switch takes effect on the next page load without forcing a re-login. Password hashing is Node's built-in `crypto.scrypt` (`lib/auth/password.ts`), not bcrypt/argon2 — neither is in CLAUDE.md's dependency table and Node's own primitive avoids adding one. `getSessionFromRequest()` (used by route handlers, via `lib/auth/guard.ts`) parses the `Cookie` header directly instead of calling `next/headers`'s `cookies()`, which incidentally means `PATCH /api/w/[workspace]`'s handler function is callable directly in a plain Vitest test with a constructed `Request` — no Next request-scope needed — though the actual PermissionGate gate-item test (`e2e/permission-gate.spec.ts`) still exercises it over real HTTP via Playwright's `request` fixture.
- `[A6]` **One real Owner-only mutation, wired end-to-end, rather than a no-op stub.** `PATCH /api/w/[workspace]` (rename workspace) is the single concrete route this session built to prove "`<PermissionGate>` hides **and** the route handler rejects — never one without the other": `components/shell/workspace-switcher.tsx` hides its "Rename workspace" menu item behind `<PermissionGate requires="owner">`, and `lib/auth/guard.ts`'s `requireRole` independently rejects the same action server-side. `/settings` itself stays a stub page per the spec's "stub routes stay stubs" — this lives in the workspace switcher, not a settings form, specifically so it didn't need to build settings UI to have a real permission boundary to test.
- `[A6]` Added `drizzle-orm`, `postgres` (the `postgres.js` driver — no other Postgres client is named anywhere, and it's Drizzle's own documented default pairing), `drizzle-kit`, and `tsx` (dev-only, for running `db/seed.ts`/`db/migrate.ts` as plain TypeScript with `tsconfig.json` path-alias support — the same "add tooling not named in the stack table when there's no in-table alternative" precedent A2 set for Storybook/Playwright/Vitest). No `@hookform/resolvers`: login/signup forms call `loginSchema`/`signupSchema`'s `.safeParse()` directly inside `onSubmit` and feed issues into RHF's `setError`, rather than adding a resolver-adapter package for two two-field forms.
- `[A6]` **`lib/repos/` has repos only for what this session's features actually query** (`workspaces.ts`, `users.ts`, `agents.ts` — the last one solely so the RLS proof test goes through the repo layer like real app code would, per "no Drizzle import outside `lib/repos`"). `agent_runs`, `run_steps`, `copilot_threads`, `copilot_messages`, `tool_calls`, `priority_items`, and `activity_events` are fully seeded (Tracks B/C/D read them later) but have no repo yet — every A6 route is a designed stub that doesn't query them, so a repo with no caller would be exactly the kind of unrequested scope CLAUDE.md says not to build. `db/seed.ts` itself writes to every table directly via `db/schema.ts` + `lib/repos/db.ts`'s `withScope()` (not through `lib/repos/*`), since it's a one-time infrastructure script that needs to populate tables no feature-facing repo exists for yet, not application code.
- `[A6]` A Server Component page (`app/(app)/w/[workspace]/*/page.tsx`) can't pass a Lucide icon component as a prop to a Client Component — React only allows already-rendered elements or serializable values across that boundary, and a bare component reference is neither (`next build` fails with "Functions cannot be passed directly to Client Components"). `components/patterns/stub-page.tsx` (`"use client"`) takes a `navId` string instead and looks the icon up itself from `components/shell/nav.ts`'s `NAV_ITEMS` — which also keeps a stub route's icon identical to its sidebar entry by construction rather than a second, driftable copy.
- `[A6]` `pnpm dev`'s on-demand per-route compilation can take well past Playwright's 30s default the first time several parallel workers each hit a different not-yet-compiled route at once — a dev-mode-only cost (a production build has none of this), not app latency, confirmed by rerunning the same suite `--workers=1` and watching it pass immediately. `playwright.config.ts`'s global `timeout` is now 60s; the six-stub-route test additionally sets `test.setTimeout(150_000)` since it cold-visits all six routes in one test.
- `[A6]` Seed workspace is **Meridian Ops** (`/w/meridian-ops`) — an AI automation agency running four agents for three clients (Lumen Dental Group, Harbor & Vine, Crestpoint Realty). All three seeded users share the password `demo-password-1234` (owner `priya@meridianops.com`, admin `jordan@meridianops.com`, member `sam@meridianops.com`) — `pnpm seed`'s own console output prints this, so it doesn't need to live anywhere else.
- `[B1]` **Why the store split is physical, not just selector discipline.** A single Zustand store with disciplined per-field selectors (`useStore(s => s.nodes)`) would *also* avoid re-rendering node components on a viewport change, in isolation — but the spec's own note flags that by B5 a trace scrubber will write to viewport 60 times a second, and every one of those writes notifies every subscriber of that store's `setState`, not just the ones whose selected slice changed (Zustand bails out via reference-equality *after* re-running each selector, not before). Two physically separate stores (`features/agents/store/canvas-store.ts` for viewport/selection-mirror/interaction-mode/clipboard, `features/agents/store/graph-store.ts` for nodes/edges) make it structurally impossible for a future broad selector anywhere to reintroduce that coupling — panning can't touch graph-store's subscriber list at all, not just "shouldn't." `canvas-store` skips the immer middleware (every field is a flat whole-value replace); `graph-store` uses it, since its node/edge array mutations (add, clone-paste, selection-flag toggling) are genuinely simpler to write against a draft.
- `[B1]` **Box-select is Shift+drag, and the additive "shift-drag adds to selection" behavior is hand-built.** With `panOnDrag` at its library default (`true`), React Flow requires holding `selectionKeyCode` (default `Shift`) to trigger box-select at all — an unmodified drag pans. Verified against the library source (`Pane`'s `onPointerMove`) that `resetSelectedElements()` runs unconditionally at the start of *every* box-select gesture, with no built-in additive mode regardless of any modifier key — so "shift-drag adds to selection" doesn't exist natively and had to be implemented in `features/agents/canvas/index.tsx`: `onSelectionStart` snapshots `event.shiftKey` and the selection as of that instant into refs, `onSelectionChange` unions the box's contents into that snapshot when the flag is set. `useGraphStore`'s `onNodesChange`/`onEdgesChange` drop `select`-type changes entirely so React Flow's own (non-additive) selection writes never fight this.
- `[B1]` GenericNode renders a `target`/`source` `Handle` at zero opacity with no pointer events, not zero handles — confirmed against the library source that a node with no `Handle` elements never gets `internals.handleBounds` computed, so edges referencing it silently fail to render (`sourceX`/`targetY` resolve to `null`). "No ports or connections" is satisfied by `nodesConnectable={false}` on the root `<ReactFlow>` (disables drag-to-connect globally) rather than by omitting handles, since the benchmark gate needs 400 real rendered edges.
- `[B1]` `deleteKeyCode={null}` — Backspace/Delete is disabled this session even though React Flow defaults it on, since there's no undo-integrated delete path yet (`useUndoable` wiring is B3's job) and an un-undoable delete on a canvas is a real data-loss trap, not a harmless default to leave in place.
- `[B1]` Dot grid tokens: pane backdrop is `--ink-050` (its own tokens.css comment says "canvas backdrop"), dots are `--ink-400` ("border-strong"). `--ink-050` against `--ink-000`/itself is a ~1.06:1 luminance delta — visually nonexistent as a dot color, so the spec's "using `--ink-050`" reads as *the backdrop* using that token, with dots needing their own (also token-derived) contrast. Passed straight into `<Background>`'s `color`/`bgColor` props as `var(...)` strings rather than through a CSS override — the component accepts any CSS color value there, and it already multiplies `gap`/`size` by the live zoom transform internally, so "scaling with zoom" needed no extra code.
- `[B1]` React Flow theming: only `@xyflow/react/dist/base.css` is imported (structural/layout rules only), never `style.css` (which layers a full default color theme on top via literal hex values baked into `.react-flow`/`.react-flow.dark` class rules). `app/globals.css` adds one small block overriding the *non*-`-default` custom properties (`--xy-edge-stroke`, `--xy-selection-background-color`, etc.) from our tokens — React Flow's own CSS reads `var(--xy-x, var(--xy-x-default))`, so setting the non-suffixed name wins without touching the library's baked-in fallback values at all. Controls and MiniMap are themed entirely through component props instead (no CSS needed there).
- `[B1]` Context menu ships a working **copy/paste** (⌘C anywhere a node is selected; Paste in the menu, anchored at the right-click point; ⌘V, anchored at a fixed +40/+40 offset from the originals) even though the spec's scope list only names "paste" — a Paste item with no way to ever populate the clipboard would be a permanently-disabled dead menu entry, which reads as more of a half-finished feature than the small, self-contained clipboard this adds. Clipboard lives in `useCanvasStore` (not `useGraphStore`) since it's ephemeral interaction state, not part of the persisted graph B3's autosave will eventually serialize. `features/agents/nodes/generic-node.tsx`'s `cloneGenericNodes` is the one place the id-regeneration/relative-offset math lives, shared by both paste entry points.
- `[B1]` "Fit view" (⌘0 and the context menu / Controls button) and "Reset zoom" (context menu only) are deliberately different actions — `fitView()` frames the current graph, `zoomTo(1)` resets scale only, keeping the current pan position. The spec's gate only requires ⌘0 to fit; "reset zoom" is context-menu-only since there's no obvious keyboard chord for it that isn't already claimed.
- `[B1]` No DB/repo work this session, matching "no persistence beyond in-memory": `app/(app)/w/[workspace]/agents/[id]/build/page.tsx` awaits `params` (Next 15 requires it) but never reads `id` — `lib/repos/agents.ts`'s existing `getAgentById` is intentionally not called here. Workspace membership is already enforced by the `(app)/w/[workspace]` layout above this route, which is all this page needs.
- `[B1]` **Benchmark infrastructure.** There's no in-product way to seed a 300-node/400-edge graph (nor should there be one), so `features/agents/canvas/canvas.stories.tsx` seeds `useGraphStore` directly in a story's mount effect — the same pattern A4's "twelve concurrent" FiringBar story and A5's "50,000 rows" DataTable story already established for this exact class of perf-only synthetic dataset. `.storybook/main.ts`'s stories glob gained `../features/**/*.stories.@(ts|tsx)` (A4 precedent: broaden the glob to match a session's own file layout rather than hardcode folders). `playwright.config.ts`'s `webServer` became an array with a second entry (`pnpm storybook -p 6006 --ci`) so `e2e/canvas-benchmark.spec.ts` and `e2e/canvas-interactions.spec.ts` can drive these stories directly — those two files are the "committed as a test" the gate asks for, and are the only e2e specs that don't go through the app's login/workspace flow at all.
- `[B1]` **The gate's fps figure is an automated proxy, not a human reading the Chrome DevTools Performance panel** — this environment has no interactive DevTools session available. `e2e/canvas-benchmark.spec.ts` installs an in-page `requestAnimationFrame` sampler before 10 seconds of scripted pan+drag+wheel-zoom, then computes the p95 frame time from real, wall-clock frame-delivery timestamps (not simulated) and asserts `>=55fps`. Recorded number, current build: **p95 frame time 16.80ms → 59.5fps**, sampled over ~600–610 frames, DOM node count during the run: **36 of 300 mounted** (well under the 100-node virtualization threshold). Gate item 2 (zero node re-renders while panning) is verified the same way — `GenericNode` carries a `data-render-count` ref-backed attribute (harmless in production, not logging anything) read before/after an isolated, zoom-free pan gesture, asserted unchanged. That isolation matters: the full 10-second pan+zoom loop deliberately drifts the viewport (that's the realistic scenario the fps figure needs), which can *legitimately* virtualize a given node in and out of the DOM — a correct remount, not a same-instance re-render, and not something a simple before/after attribute comparison across that longer gesture could distinguish from a bug. See Deferred below for what a human still needs to confirm.
- `[B1]` Playwright's own `page.mouse.wheel`-based zoom and the benchmark's pan-drag both need a screen point guaranteed to land on empty pane space, not a node — otherwise the gesture grabs and drags whatever node happens to be under the cursor instead of panning the canvas (caught by the render-count check itself firing on an unrelated node: dragging one *should* re-render it, just not the one being measured). The benchmark story's grid spacing (220px columns / 168px node width, 140px rows / 44px node height) makes flow point `(200, 100)` a gap on both axes regardless of which cell it falls in mod the spacing.
- `[B2]` **Port type identity is achromatic (an icon per type: Type/Braces/FileText/Zap/Asterisk for text/json/document/signal/any), never a color.** CLAUDE.md's five semantic hues each already have exactly one meaning (emerald/amber/red/blue/violet), none of which is "port data shape," and hard rule 4 forbids decorative reuse — assigning port-type colors from that same palette would either invent a sixth meaning per hue or collide with an existing one (e.g. a "text" port rendered in `red` would read as "this port is broken"). Ports are small circular `Handle`s absolutely-positioned as edge overlays (see next entry), not a layout row, with the icon inside and the label as a native `title` tooltip.
- `[B2]` **NodeShell renders ports as absolutely-positioned overlays on the card's own left/right edges (evenly spaced via a computed `top: %` per port, not React Flow's default same-position stacking, which needs manual spacing for >1 handle per side), not as a labeled row that grows the card.** Card height is therefore constant (header + one-line summary only, ~70px) regardless of port count, keeping every node close enough to B1's `GenericNode` footprint that B1's benchmark grid spacing (220×140) and the Interactive story's box-select geometry didn't need to change. An earlier draft rendered ports as full labeled rows (icon + text label inline) — visually clearer but grew tall nodes to ~140–150px, which would have required re-deriving both the benchmark's "guaranteed empty pane point" math and the box-select e2e test's selection-rectangle bounds; the overlay approach was chosen specifically to avoid that blast radius.
- `[B2]` **Cycle rejection is a post-drop toast, not a pre-drop `isValidConnection` block.** `isValidConnection` (checked on every hovered handle during a drag, cheap) only gates port-type compatibility — the visual desaturation + hover reason gate item 2 asks for. Cycle detection needs the full edge list (a graph-global property) and is deliberately not run on every hover; instead `onConnect` re-validates (types + cycle) and, on a cycle, shows a `danger` toast naming every node on the loop (`This would create a cycle: A → B → C → A.`) without adding the edge. The keyboard connection path (gate item 6) takes a different, complementary approach: cycle-forming targets are filtered out of the candidate list *before* they're ever offered, so `Enter` there always succeeds — a mouse drag can attempt (and get rejected from) a cycle; the keyboard path can't get into one to begin with. Both surfaces share the same `validateConnection` (features/agents/lib/validation.ts) — only how each surface reacts to a `false` result differs.
- `[B2]` **The keyboard connection state machine (`idle` → `port-focus` → `connecting`) lives in its own tiny external store** (`features/agents/lib/keyboard-connection-store.ts`, `useSyncExternalStore` + a module-level object — same shape as Toast's queue and CommandPalette's registry), not as a field on `useGraphStore`/`useCanvasStore`. It's ephemeral UI state with no persistence value and a keypress-driven update frequency utterly unlike either store's real concern (graph data; 60Hz viewport). The capture-phase (`addEventListener(..., true)`) document keydown listener in `canvas/index.tsx` is required, not stylistic: React Flow binds its own default arrow-key node-move handling directly on the focused node in the bubble phase, and only a capture-phase listener calling `stopPropagation()` runs early enough to reliably preempt it before the node ever sees the arrow key.
- `[B2]` **Real bug found via this session's own testing, not left for someone else: `useGraphStore`'s `setSelection` mutated `node.selected` in place, which immer resolves to *the same object reference* for any node whose value didn't actually change.** React Flow resets its own internal "is this node selected" bookkeeping the instant a new box-select drag starts (a fact B1 already documented for a different reason) — that reset never reaches our store (`onNodesChange` drops `select`-type changes), but it does mean React Flow's internal copy and ours briefly disagree. When a *second*, additive box-select drag re-confirmed a node that was already selected, immer kept that node's old object reference (no value changed from our side), and React Flow — seeing a reference-identical node object — had nothing to prompt it to re-sync from our (correct) value over its own (stale, reset-to-false) internal one: the store's `.selected` flags were verified correct via direct inspection, but the rendered `.selected` CSS class silently didn't update. Fixed by having `setSelection` rebuild every node/edge object unconditionally (`state.nodes.map(node => ({...node, selected: ...}))`) rather than mutating in place, forcing React Flow to re-sync every time regardless of whether the value nominally changed. Caught by `e2e/canvas-interactions.spec.ts`'s existing additive shift-drag test — it was passing in B1 only because B1's `GenericNode` era never had a *second* additive drag re-confirm an already-true node in the same session as immer's no-op optimization; this session's added tests didn't create the bug, they just were the first to exercise the sequence that reveals it.
- `[B2]` **The Node Library panel docks at the pane's top-left corner (`<Panel position="top-left">`, inside `<ReactFlow>`), and this genuinely occupies real screen space other canvas interactions need to route around** — both `e2e/canvas-interactions.spec.ts`'s box-select test (whose original top-left-to-bottom-right drag literally started from underneath the new panel) and this session's own `Connections` story (whose nodes were first placed at flow x=60, also underneath it) hit the same collision. Fixed by dragging bottom-right-to-top-left instead (identical resulting selection rectangle, different mousedown corner) and by moving the `Connections` story's nodes to x≥350. This is expected, not a bug to design away — a docked palette overlapping canvas content by design is standard for this class of tool (Figma, n8n); the fix is routing test geometry around it, the same way a real user would pan the canvas first.
- `[B2]` **LLM's `context` input port is typed `document`, not `json`** — the standard RAG shape (a Knowledge node's `results` output feeds an LLM's context directly) *and* what makes the spec's own canonical example ("Connecting `text` → `document` is impossible") concretely reachable with real, shipped node types: no other node type has a `document`-typed input, so without this, that exact example could be described in the abstract (port-types.ts's compatibility function has no special case for it) but never actually attempted against two real nodes. `e2e/canvas-connections.spec.ts` uses two LLM nodes for exactly this (`llm-a.response` (text) → `llm-b.context` (document) rejected; → `llm-b.prompt` (text) accepted).
- `[B2]` No model-name string literal appears in `features/agents/nodes/types/llm.tsx` (or anywhere else in this session) per CLAUDE.md's LLM provider rule — `lib/llm/` doesn't exist yet (a later Copilot-track session's job), and the LLM node's config schema (`systemPrompt`, `temperature`) deliberately omits a `model` field rather than default it to a placeholder string, since this session has no execution and no config-editing UI to make that field meaningful yet.
- `[B2]` **The registry test proves the generic-consumption property, not literal zero-file-touch extensibility.** `features/agents/nodes/registry.ts` statically imports and registers all ten shipped types (unavoidable without a fragile bundler-specific auto-discovery mechanism — Turbopack/webpack don't offer a reliable equivalent of Vite's `import.meta.glob` here); adding an eleventh type means adding its file *and* one import + one `registerNodeType(...)` call in `registry.ts`. What `features/agents/nodes/registry.test.ts` actually proves is the property that matters: a definition registered with zero other code changes (no new component, no new branch in the library panel, search, or NodeShell) is immediately findable via `getNodeType`/`listNodeTypes`/`searchNodeTypes` and renders correctly through the one shared `NodeShell`, because nothing downstream special-cases a node type by id. The one caveat: `registry.ts`'s exported `nodeTypes` map (the one thing `<ReactFlow nodeTypes={...}>` needs) is built once at module load from whatever's registered *by then* — true for all ten shipped types, and true in practice for any real eleventh type too, since a new type is always a file imported once at build time, never something invented at pure runtime after the app has already loaded.
- `[B2]` Node/edge ids use `crypto.randomUUID()` (`node-<uuid>` / `edge-<uuid>`) rather than B1 GenericNode's counter+`Date.now()` scheme — simpler (no module-level mutable counter to reason about) and unconditionally collision-free, which matters more now that copy/paste/duplicate can mint many ids in the same tick from three different call sites (`registry.ts`'s `createNode`/`cloneNodes`, the keyboard connection path's edge creation).
- `[B2]` `generic-node.tsx` (B1's placeholder) is deleted outright, not kept as a fallback/alias node type — every session-B1 story and the empty-state's "Add a trigger" action were updated to use real registered types (`trigger`, and a mix of the ten across the Interactive/Benchmark stories) instead.
- `[B3]` **User decision: added `lib/llm/models.ts` + a `model` field on the LLM node, ahead of `lib/llm/provider.ts`.** Gate item 7 explicitly names "changing the model" on 3 selected LLM nodes, but B2 deliberately left `model` off the LLM config schema (CLAUDE.md: "no model name string literals outside `lib/llm/models.ts`", and that file didn't exist yet). Asked the user rather than inventing a workaround; chose adding the minimal literal-holding file now (`GROQ_MODELS`/`GROQ_MODEL_IDS`/`DEFAULT_GROQ_MODEL`/`getModelLabel`) over silently reinterpreting the gate against `temperature` instead. `lib/llm/provider.ts` (the actual Groq client) still doesn't exist — out of scope here, later Copilot-track work.
- `[B3]` **`agents.graph_jsonb` needed a real schema migration the spec named but didn't provide** — `db/schema.ts` gained `graphJsonb`/`viewportJsonb` jsonb columns on `agents` (distinct from `agentVersions.graph`, an immutable versioned snapshot bumped on publish, not on every autosave), migrated via `db/migrations/0002_peaceful_mac_gargan.sql`. Viewport rides in its own column since gate item 6 explicitly requires restoring it, and `@xyflow/react`'s `Viewport` shape is too small to deserve its own table.
- `[B3]` **`app/api/agents/[id]/route.ts` takes `workspaceSlug` in the PATCH body and reuses `requireRole(request, workspaceSlug, "member")`** — the same guard the workspace-rename route uses — rather than trusting a client-supplied `workspaceId` directly. `withScope`'s RLS scoping trusts the caller to have already verified membership (see A6's decisions); a raw `workspaceId` from the client would let RLS "succeed" against a workspace the user isn't actually a member of, since `agents`' policy (unlike `members`') has no `user_id` fallback clause. `requireRole`'s minimum rank ("member") is used, not "owner" — nothing in this session's spec gates agent editing behind a role. The same PATCH schema (`lib/repos/agents.ts`'s `AgentUpdatePatch` / the route's `patchSchema`, a `.partial()`-shaped body) serves both the debounced graph autosave (`graph`+`viewport`) and the Inspector's agent-settings form (`name`+`description`), rather than two endpoints.
- `[B3]` **Undo registration lives in `features/agents/lib/graph-undo.ts`, called as plain functions from canvas/index.tsx, context-menu.tsx, library/index.tsx, and the Inspector — not through `useUndoable()`.** `useUndoable()` is a hook (fine inside a component body) that just wraps two plain functions (`pushUndoEntry`, `undo`, both from `lib/undo/store.ts`) plus a `toast()` call and a `useUndoShortcuts()` binding; `registerGraphUndo()` replicates the first three directly so call sites that aren't naturally component-shaped (a keydown handler's inner branch, a store-adjacent helper) don't need a `registerUndo` callback threaded through props. `useUndoShortcuts()` itself is still called once, directly, in `canvas/index.tsx`'s `CanvasInner` (ref-counted, harmless if also mounted elsewhere) to guarantee the global ⌘Z/⌘⇧Z binding.
- `[B3]` **Property-edit coalescing (gate item 3's "typing a 12-character prompt is a single undo step") is a separate mechanism, `commitCoalescedEdit`/`flushCoalescedEdit` in the same file** — a module-level `pending` object (key, label, before, latest, timer) that live-applies every call's `after` value immediately (so the canvas summary/field updates on every keystroke) but only pushes one `registerGraphUndo` call per 300ms-idle burst, using the value from the *first* call in the burst as `before` and the *last* call's value as `latest`. A burst starting on a different `key` (a different field, or a different node selection) flushes whatever was pending first. `graph-undo.test.ts` proves this against real timers (not `vi.useFakeTimers()`, since `pending` is module state shared with whatever test runs next — fake timers left unflushed would leak a scheduled callback across files). The Inspector flushes on node-selection change and unmount so a mid-edit burst never gets silently orphaned or merged into a different node's edit.
- `[B3]` **Node-move undo batches an entire multi-node drag gesture into one step.** React Flow's `onNodesChange` delivers one `position`-type `NodeChange` per dragged node per call, all in the same call for a multi-selection drag; `canvas/index.tsx`'s `handleNodesChange` wraps the store's raw `onNodesChange`, capturing each node's pre-drag position on its first `dragging: true` sighting (a `Map` ref) and diffing against the final `dragging: false` position, then registers exactly one `registerGraphUndo` call covering every node that actually moved in that gesture — not one call per node.
- `[B3]` **Delete/Backspace handling, deferred by B1 with `deleteKeyCode={null}` specifically because it had no undo path yet, is implemented this session** in the same bubble-phase keydown effect as the other shortcuts (copy/paste/duplicate/select-all), `isTypingTarget`-gated. Deleting selected nodes removes them and every edge touching them as one `removeNodesWithEdges` call and one undo entry (`restoreFragment` puts both back together on undo); deleting only selected edges (no nodes selected) is a separate "Disconnected nodes" undo entry via `removeEdgesByIds`. `deleteKeyCode={null}` stays on `<ReactFlow>` — React Flow's own deletion still never runs; this session's handler is what actually deletes.
- `[B3]` **`features/agents/lib/scope.ts`'s `resolveUpstreamScope` walks the *entire* upstream ancestry, not just direct predecessors** — a plain BFS over `edges` keyed by `target` (the mirror of `cycle-detect.ts`'s forward-keyed adjacency), visited-set-guarded against a cycle turning it into an infinite walk even though the canvas itself rejects cycles on connect. Each ancestor's registered output ports (via `getNodeType`) become `ScopeVariable`s; the `{{token}}` a variable inserts is `slugify(node.label).portId`, with a numeric suffix (`_2`, `_3`, ...) if two upstream nodes share a label after slugifying. Kept framework-free (no React/@xyflow/react/zustand import) and unit-tested directly per the spec's own note that it'll be reused by the automation builder later.
- `[B3]` **Field-to-control mapping is inferred structurally from each config field's Zod type, not from field-level metadata on `NodeTypeDefinition`** (which stays data-only per B2's decision): `ZodEnum` → Select, `ZodBoolean` → Switch, `ZodNumber` with both a min *and* max (via zod's own `.minValue`/`.maxValue` getters) → Slider, otherwise → Input; a `ZodString` field whose key matches `/prompt|expression/i` → a Textarea with variable autocomplete, everything else → a plain Input. The one exception: the LLM node's `model` enum renders its Groq id options through `lib/llm/models.ts`'s `getModelLabel` (`gemma2-9b-it` → "Gemma 2 9B") rather than the raw enum value, since it's the one enum in this codebase where the id itself isn't meant to be read — every other enum (`triggerType`, memory's `scope`) already uses human-readable values.
- `[B3]` **Inspector's "Advanced" section is exactly the existing disabled/enabled Switch, moved here from where B2's context menu already exposed it — nothing else.** The spec names three sections (identity/configuration/advanced) but no config field in any of the ten node types carries an "advanced" designation; rather than inventing schema-wide field-tier metadata for a session that shouldn't grow the schema surface, Identity got Name (single-node only; multi-select disables it and shows "Mixed values") + read-only Type, Configuration got every schema field, and Advanced got the one flag that already existed and fits the label. Advanced's disable toggle is deliberately **not** undo-registered — it isn't in the spec's item 6 list (`add/delete/move/connect/disconnect/edit property/paste`), and it already shipped without undo in B2's context menu.
- `[B3]` **Multi-select shared-property editing (gate item 7) shows "Mixed values" (placeholder text, empty control) for any field where the selected nodes disagree**, rather than defaulting to the first node's value silently. Editing a mixed or uniform field applies the new value to every selected node via one `setNodeConfigValues([{nodeId,key,value}, ...])` call and registers one undo entry whose `undo` closure restores each node's own original (possibly differing) value and whose `redo` re-applies the shared value to all — both directions go through the same store action, just with different per-entry values. Mixed-*type* selections (not all nodes sharing one `type`) don't attempt a form at all — a short explanatory message instead, since "shared properties" has no meaning across different config schemas.
- `[B3]` **`agents.graph_jsonb`'s hydration into the client-only `useGraphStore`/`useCanvasStore` singletons happens in a `useLayoutEffect` inside `CanvasInner`, gated by new optional `initialGraph`/`initialViewport` props on `Canvas`** — not by mutating the stores during render. Those stores are plain module-level Zustand singletons with no per-request scoping; writing to them synchronously during a Server Component's render would leak between concurrent requests in the same Node process. `initialViewport` is *additionally* threaded straight into `<ReactFlow defaultViewport={...}>` as a real prop (not read from the store) because React Flow only reads `defaultViewport` once, on its own first render — a parent's layout effect (which fires after children render) is structurally too late for that specific prop, even though it's not too late for the store-driven `nodes`/`edges`, which `CanvasInner` does subscribe to reactively.
- `[B3]` **`features/agents/builder.tsx` (`AgentBuilder`) is a new, small client component composing `Canvas` + `Inspector` side by side** — not named in the spec's file list, but required to actually wire the two together into one screen; `page.tsx` (a Server Component) re-resolves `getSession()`/`getWorkspaceContext()` itself rather than receiving them from the `(app)/w/[workspace]` layout above it, matching every other page in this app (Server Components each fetch what they need; Next.js has no built-in layout→page data channel here).
- `[B3]` ~~Environment flakiness, not a code defect~~ **— corrected in B4: this was a real, reproducible bug, not sandbox flakiness.** A long-running `pnpm dev` process in this sandbox intermittently threw `PostgresError: invalid input syntax for type uuid: ""`; at the time this looked like it only affected a route's *first* request after a fresh compile, self-healing on retry, so it was written off as dev-mode/connection-pool flakiness (the standalone `tsx` repro attempts that "always succeeded" were single-shot scripts that never happened to hit the real trigger condition). B4 found the actual trigger — see B4's own Decisions entry below — and fixed it at the RLS-policy level (`db/migrations/0005_fix_rls_empty_string_cast.sql`). Left here, struck through rather than deleted, so a future session searching this file for that error message finds the real explanation instead of re-diagnosing it as flakiness a second time.
- `[B4]` **A real, reproducible Postgres bug found and fixed: every RLS policy's `current_setting(..., true)::uuid` cast could crash on an unrelated, unscoped query.** Root cause: `current_setting(name, missing_ok)` only returns NULL when Postgres has *never once* seen that custom GUC name on the current backend connection — `lib/repos/db.ts`'s `withScope()` calls `set_config('app.workspace_id', ..., true)` (transaction-*local*) whenever a caller scopes by `workspaceId` alone (no `userId`), and once that's happened even once on a pooled connection, the GUC is "known," so a *later* transaction on that *same reused connection* that never sets it again gets back `''` (empty string), not NULL, the instant the earlier transaction's local scope expires at commit. `''::uuid` throws, not "zero rows." Reproduced deterministically standalone in under 15 lines: eight rapid `withScope({ workspaceId })` calls followed immediately by one `withScope({ userId })` call (`lib/repos/workspaces.ts`'s `getWorkspaceContext`) on the same process — every time. Never triggered before B4 because no earlier session's code opened several rapid, alternating single-key `withScope()` calls on a shared connection in one request; B4's run route is the first (`createRun` + up to N `recordStep` + `finishRun`, all `{ workspaceId }`-only, back to back). **Fixed at the RLS layer**, not by changing `withScope` itself: `db/migrations/0005_fix_rls_empty_string_cast.sql` wraps every policy's cast in `NULLIF(current_setting(...), '')::uuid`, converting the empty-string case back to a real NULL — restores the exact original fail-closed behavior (an empty/unscoped axis still can never match a real row) rather than loosening it; verified with a direct unscoped-query check (0 rows) after the fix, plus the existing RLS proof tests (`lib/repos/agents.test.ts`, `lib/repos/agents-graph.test.ts`) still pass. B3's "environment flakiness" diagnosis (see the struck-through entry above) was wrong — this bug has been live since A6 introduced RLS, just never hit by any prior session's query pattern.
- `[B4]` **User decision, asked mid-implementation:** whether `run_steps`' `run_step_kind` enum needed to cover all ten node types (only `llm_call`/`tool_call`/`condition`/`transform` existed from A6's seed schema) was unambiguous once the spec's "every node execution writes a run_steps row" was read literally, so no question was needed there — but the *existing seeded `run_steps` rows'* new `NOT NULL node_id` column had no natural value (A6's fixture runs were never tied to a real graph). Backfilled with a synthetic `'legacy-' || id` placeholder in the migration itself (`0003_concerned_colleen_wing.sql`) rather than asking, since it's clearly fixture data and the alternative (blocking the migration on a schema decision) wasn't a real tradeoff.
- `[B4]` **`run_steps.cost_cents` is `numeric(10,6)`, not a plain integer**, despite the spec's own wording ("cost in cents"). A short test-console prompt costs a fraction of a cent at Groq's real per-token rates (confirmed against the live API: a 73-input/21-output-token call cost 0.005966¢) — rounding to whole cents would collapse nearly every real run this console produces to 0, making gate item 7's "within 5% of Groq's reported usage" comparison meaningless for exactly the size of run this feature runs. The unit is still cents (matching the spec's wording), just not rounded to a whole one.
- `[B4]` **`lib/llm/models.ts`'s original two B3-era model ids (`mixtral-8x7b-32768`, `gemma2-9b-it`) are gone from Groq** — confirmed via `GET /openai/v1/models` against the real key in `.env.local`; gate item 1 needs an actual successful Groq call, so a listed-but-dead model isn't hypothetical. Replaced with two more models confirmed live in that same catalog response (`openai/gpt-oss-20b`, `openai/gpt-oss-120b`), alongside the two originals that are still live. Added real per-token pricing (`promptPricePerToken`/`completionPricePerToken`, copied from that same catalog's `pricing` field) and `estimateCostCents()`, since B4's cost accounting needs real, model-specific rates, not a single blended guess.
- `[B4]` **No `groq-sdk` dependency added** — `lib/llm/groq.ts` is a hand-written `fetch()`-based streaming client against Groq's OpenAI-compatible chat completions endpoint. Verified against the real API that a streaming response's token usage arrives two ways (`x_groq.usage` on the `finish_reason` chunk, and again as a final standalone chunk when `stream_options.include_usage` is requested) — the client reads whichever arrives first, since both carry the same `prompt_tokens`/`completion_tokens`. Keeps `lib/llm/` free of a dependency not in CLAUDE.md's table, and "No `groq-sdk` import in this feature" (the spec's own words) becomes automatically true rather than needing enforcement.
- `[B4]` **NDJSON over the wire, not SSE.** `app/api/agents/[id]/run/route.ts` streams a `ReadableStream` of `\n`-delimited JSON objects, read client-side via `response.body.getReader()` + manual line-splitting. `EventSource` (the natural SSE client) is GET-only and can't carry a request body, but the run needs one (the trigger's resolved input from the console's input form) — a POST + manual stream reader needs no new dependency on either side, unlike an SSE polyfill or the `ai` package (neither is in CLAUDE.md's table).
- `[B4]` **Cancellation has no separate channel** — the test console's Cancel button calls `AbortController.abort()` on the same `fetch()` driving the run; Next's Route Handler surfaces that as `request.signal` firing, which is threaded straight through `runGraph`'s `signal` param into `groq.ts`'s own `fetch(..., { signal })`. One real in-flight Groq HTTP request is what actually stops, not a synthetic "give up early" flag — verified with a real Groq call, aborted deterministically right as the LLM step starts (not time-based): the client's own read loop ends within ~5ms of calling `abort()`, and the DB shows the run as `cancelled` with only the three already-completed steps recorded (no row at all for the aborted LLM step, not a `failed` one) — comfortably inside the 500ms budget.
- `[B4]` **Node executors are pure per the spec's own note ("keep side effects in the orchestrator so executors stay unit-testable without a database") — with the LLM executor's Groq call as the one deliberate exception**, since a network call is unavoidable for that node type to do anything real. Every other executor (`trigger`, `tool`, `condition`, `loop`, `memory`, `knowledge`, `transform`, `output`, `humanInLoop`) is a synchronous, side-effect-free function of `(config, resolvedInput, ctx)` — DB writes happen exclusively in `app/api/agents/[id]/run/route.ts`'s `onStepEnd` callback, never inside `features/agents/engine/`, so the whole orchestrator (`executor.test.ts`) is tested with zero DB and zero HTTP mocking except for the one test that deliberately mocks `lib/llm/provider`.
- `[B4]` **No real knowledge base, tool registry, or human-approval UI exists anywhere in this codebase** (knowledge base is explicitly out of this whole build's scope per CLAUDE.md; a tool registry is C3's job; approval UI doesn't exist yet either) — so `knowledge.ts`, `tool.ts`, and `human-in-loop.ts`'s executors are honest, deterministic stand-ins (empty results / an echo-back envelope / auto-approve) rather than either throwing (which would make any graph containing these node types unconditionally fail) or fabricating plausible-looking fake output. `humanInLoop` auto-approving specifically means it branches exactly like `condition` (only the `approved` output port fires), so a graph using it still produces a coherent, fully-traced run today; real pause/resume is a future session's job, not a B4 gap to work around silently.
- `[B4]` **Loop does not really iterate.** A true loop would mean the orchestrator re-entering already-visited nodes once per item — a materially bigger feature (per-iteration step recording, subgraph re-execution) than a single-pass DAG walk supports, and none of B4's gate items exercise iteration count. `loop.ts`'s executor caps `items` at `maxIterations` and passes the (possibly truncated) array straight through as `item`, firing `done` immediately — single-pass, not a design a future session extending real iteration needs to work around, since `done` firing alone is still a coherent "the loop finished" signal either way.
- `[B4]` **`condition.ts`'s expression evaluator is a small hand-rolled recursive-descent parser (literals, `==`/`!=`/`>`/`<`/`>=`/`<=`, `&&`/`||`/`!`, parens) — deliberately not `eval`/`new Function`.** By the time a Condition node's `expression` reaches this executor, the orchestrator has already replaced every `{{token}}` with its real resolved value's literal text (`scope-resolver.ts`'s `interpolateConfig`), so the evaluator only ever needs to parse literals and comparisons, never variable lookups — arbitrary code execution from a text field isn't worth the risk for that. `transform.ts` similarly avoids `eval`: a dot-path accessor only (`resolvePath`), matching "reshaping arbitrary data" without needing a general expression language.
- `[B4]` **The orchestrator's branch-pruning contract: a `NodeExecutionResult.output` port simply absent from the returned object means "did not fire."** `Condition` returns only `{true: ...}` or `{false: ...}`, never both; `humanInLoop` returns only `{approved: ...}`. `shouldSkip()` (executor.ts) marks a node `skipped` only when *every* incoming edge is dead (source failed/skipped, or its port never fired) — a node fed by both a live and a dead branch still runs, the natural "OR merge" semantics for a convergence point (exercised directly: the seeded Recall Scheduler's Output node is reachable via both `condition.true` → `tool` → `output` and, structurally, `condition.false` → `output` directly, though only one path ever actually fires per run).
- `[B4]` **`db/seed.ts`'s new 6-node graph (trigger → transform → knowledge → llm → condition → output) needed a `transform` bridge node the obvious `trigger → knowledge` chain can't skip** — a real consequence of B2's port-type system, not a demo shortcut: Trigger's only output is `signal`, and Knowledge's `query` input is `text` (not `any`), so `arePortTypesCompatible` rejects a direct wire; the graph seeds through Transform (both ports `any`) instead, exactly as a real user wiring this in the Inspector would have to. Applied to the live seeded "Recall Scheduler" agent's `agents.graph_jsonb` (not `agentVersions.graph`, which stays the empty-array snapshot every seeded agent has — see B3's decision on why `graph_jsonb` is the one live-editing/running target); the other three seeded agents' graphs stay empty, matching their own narrative (e.g. "Listing Copy Generator" is still an untouched draft).
- `[B4]` **The seeded 6-node run genuinely completes in ~500ms against real Groq** (a 73-input/21-output-token call, first token in ~150-210ms measured repeatedly) — good news for gate item 3's "<1.5s to first token," but it means `e2e/agent-run-console.spec.ts` deliberately does *not* try to assert on a mid-run `"running"` DOM state: a multi-step Playwright assertion chain against a real network call that fast is a flaky race, not a meaningful test. The test instead verifies the settled end-state (every node reaches `data-run-status="succeeded"`, the FiringBar clears, the step log and output panel show the real content) — proven live in a real browser — while the *mechanism* for the transient "running" state (`onStepStart` → `useRunStore.getState().setNodeStatus(nodeId, "running")`, read by `NodeShell`) is simple, directly-readable code rather than something that needs a screenshot to be credible, matching this codebase's own B1 precedent for gate items where the live instant is real but not reliably catchable headless.
- `[B4]` `data-node-type`/`data-run-status` live on `NodeShell`'s own root `<div>`, which is a *child* of React Flow's own auto-generated `.react-flow__node` wrapper (which carries only `data-id`) — not the same element. A first draft of the e2e test wrote `.react-flow__node[data-run-status]` (combining both on one selector) and got a persistent, non-flaky "0 elements" failure; fixed by querying `[data-run-status]` alone. Noted here since it's an easy trap for a future session's test to fall into again.
- `[B4]` `features/agents/canvas/run-edge.tsx` (`RunEdge`, the traveling-pulse edge) is registered as the `"default"` edge type on `<ReactFlow edgeTypes={{ default: RunEdge }}>` — every edge in this app already goes through this one type (nothing sets a per-edge `type`), so this replaces the built-in bezier edge everywhere rather than needing an opt-in. The pulse itself is a plain SVG `<animateMotion>` riding the same bezier path `<BaseEdge>` draws — no framer-motion loop needed to move a dot along a fixed path, and it's naturally absent entirely under reduced motion (no dot rendered, not a separate reduced-motion branch to maintain).
- `[B5]` **The replay canvas is its own, separate `<ReactFlowProvider>` instance (`features/agents/trace/index.tsx`'s `ReplayCanvas`) — not a reuse of `features/agents/canvas`'s editable `Canvas`.** "No editing during replay" (the spec's own out-of-scope line) means it must never touch `graph-store`/`canvas-store`, the live builder's module-level singletons — those could hold a completely different agent's in-progress edits at the moment someone opens a trace, and writing to them from replay would leak state between the two screens. Replay gets its own node renderer (`ReplayNodeComponent`) and edge renderer (`ReplayEdge`), driven entirely by props and `replay-store.ts`, with `nodesDraggable`/`nodesConnectable`/`elementsSelectable` all `false`. `CanvasBackground`/`CanvasMiniMap` (B1) are reused as-is since they're genuinely presentational (no store dependency); `CanvasControls` is not, since its zoom-percent readout reads the *live builder's* `useCanvasStore` and would show the wrong number here.
- `[B5]` **`replay-store.ts` caches a wholesale-recomputed `nodeStates`/`activeEdgeIds` map, not just `playheadMs` alone — a deliberate reading of the spec's own Notes, not a violation of them.** The Notes warn against storing *per-node state and mutating it* as the playhead moves, "the reason most timeline scrubbers feel unreliable" — i.e., independently-patched state that can drift from the truth. What's cached here is different in kind: every write (`setPlayhead`, `loadTrace`, `play`) fully replaces the map via one pure call to `playhead.ts`'s `deriveNodeStatesAtTime`/`deriveActiveEdgeIds` over the single source of truth (`steps`, `edges`, the new `playheadMs`) — never an incremental patch, so it cannot desync from what it's derived from. It exists purely so `ReplayNode`/`ReplayEdge` can subscribe with a granular per-id Zustand selector (`state.nodeStates[id]`), mirroring `run-store.ts`'s live `nodeStatuses` (B4) — without it, every node would re-render on all ~60 playhead writes a drag produces per second (gate item 2), not just the one whose own status actually changed, which is exactly the class of bug B1's canvas-store/graph-store split (see B1's own Decisions) was built to make structurally impossible.
- `[B5]` **A skipped step has no recorded timestamp at all (the engine never executes it, so there's nothing to time — see B4's `shouldSkip` branch) — `playhead.ts`'s `normalizeSteps` synthesizes an instant for it at the point execution had logically reached by `stepIndex` order, i.e. right where the previous step left off**, rather than defaulting it to `t=0`. A `t=0` default would make every skipped node appear "skipped" from the very first frame of replay regardless of when the real branch decision actually happened, which is a frame-accuracy bug, not a cosmetic one. Verified directly: a real failed run's `condition`/`output` steps (skipped after the `llm` step failed) only flip from `pending` to `skipped` once the playhead passes the `llm` step's real failure instant, not before.
- `[B5]` **Cost meter snaps to the run's exact recorded total (`agent_runs.cost_usd`) once the playhead reaches the run's end, rather than trusting a running re-sum of per-step `cost_cents` to land on the same number** — this is not a hypothetical edge case. A real run in this session's own database has a step-level cost of `0.005966¢` but a run-level recorded total of `0.0001` dollars = `0.0100¢` (dollars rounded to 4 decimal places elsewhere in the pipeline, cents rounded to 6 elsewhere) — a genuine, live rounding mismatch between the two stored representations. Gate item 5 requires the meter to equal the run's recorded total *exactly* at the end; `deriveCostCentsAtTime` (`playhead.ts`) returns the partial per-step sum before the end and the authoritative `finalTotalCostCents` at or after it, so the two representations never have to agree with each other, only the second one has to be shown at the end.
- `[B5]` **An edge is "active" (carrying a payload) in the transit window between its source step's end and its target step's start — not "while the source is `running`,"** the live convention `run-edge.tsx` (B4) uses. Replay has no live "running" phase on the wire to key off of; a recorded step is only ever `succeeded`/`failed`/`skipped` at rest. The transit-gap definition is also more literally correct for what a traveling pulse is supposed to represent (data in flight *between* two nodes), and it naturally excludes edges into a `skipped` target (nothing was ever carried), which the live convention doesn't have to reason about since a skipped node's incoming edges never show `source.status === "running"` either way.
- `[B5]` **Reduced motion: `ReplayEdge`'s active state renders a static stroke highlight (thicker line, `--color-blue-fg`) instead of doing nothing, deliberately diverging from `run-edge.tsx`'s (B4) own reduced-motion behavior (no pulse, no substitute).** B4's choice was correct for B4's own gate wording ("naturally absent... not a separate reduced-motion branch to maintain"); B5's gate item 8 is explicit and different — "edge pulses are static highlights" — so reusing `RunEdge` as-is would fail this session's own gate. `ReplayNode`'s border-color/opacity transitions are bound to `useMotion()`'s resolved duration (`base` under normal motion, `instant` under reduced motion) rather than an unconditional CSS `transition-colors`, so a state change actually crossfades either way instead of only "happening not to look different" between the two modes.
- `[B5]` **A real bug found via this session's own e2e testing, not left for someone else: React Flow sets `pointer-events: none` inline on a node's own wrapper whenever the node is neither draggable nor selectable — both deliberately `false` here — and that inline style is inherited by everything inside it, including `ReplayNode`'s own click handler.** Confirmed against the library's own CSS (`.react-flow__node` sets `pointer-events: all` as a class rule, but React Flow's own inline style wins over it and is only added for exactly this draggable/selectable-both-false combination). Fixed with an explicit `pointer-events-auto` on `ReplayNode`'s root button — scoped to the one element that actually needs clicks, not a global override. Found by an e2e test hanging on `.click()` indefinitely, traced via a standalone Playwright script that walked `document.elementFromPoint()`'s ancestor chain at the click coordinates.
- `[B5]` **A second, related real bug from the same investigation: `NodeShell`'s (and, copying it, `ReplayNode`'s original) `min-w-44` has no upper bound, and `truncate` does nothing without one.** The seeded Recall Scheduler graph's columns sit 220px apart; the LLM node's summary (`getModelLabel(model) — "systemPrompt"`, B2/B3) rendered its full system-prompt sentence at ~518px wide in a real browser, visibly overlapping its neighbor and swallowing clicks meant for it (`.react-flow__node` intercepting for the wrong node, confirmed via `elementFromPoint`). Fixed in `ReplayNode` only (a fixed `w-44`, not `min-w-44`) — `node-shell.tsx` itself is untouched, since it carries the identical latent risk but is B2/B4's already-gated code, out of this session's scope to modify; worth a future session's attention if a long-summary node type is ever paired with tight column spacing in the live builder too.
- `[B5]` **Every SSR-visible run timestamp goes through a new hand-built `features/agents/lib/format.ts#formatTimestamp`, not `Date#toLocaleString()`.** Found live, not hypothetically: `toLocaleString()`'s exact output depends on the runtime's ICU data, which can differ between Node's server build and a browser's for the *same* locale — a real, commonly-hit source of React hydration mismatches for any date rendered during SSR (both `runs/[runId]/page.tsx` and `runs/page.tsx` pass real `Date`s all the way down to a `"use client"` component that renders them on first paint, so they're part of the server-rendered HTML, not something that only exists after a user interaction). `formatTimestamp` builds its string from `Date`'s local-timezone getters directly (consistent between Node and a browser on the same machine) rather than through any `Intl`/locale-formatting path, eliminating the divergence source entirely rather than papering over it with `suppressHydrationWarning`.
- `[B5]` **`db/seed.ts`'s Recall Scheduler run_steps now carry the graph's real node ids (`seed-recall-trigger` … `seed-recall-output`), not the generic `seed-{agentKey}-{stepIndex}` every other agent's fixture steps still use.** Recall Scheduler is the one seeded agent with a real `graph_jsonb` (B4); its 14 fixture runs previously recorded steps under synthetic ids that matched *no* node on that real graph, so opening any of them in B5's trace view would have replayed against a graph where nothing ever lit up. `RunPlan["steps"]` gained an optional `nodeId` field (defaulting to the old synthetic scheme) so the other three agents — which have no real graph to replay against regardless — were left untouched. Real, `agentRuns`-produced runs (via B4's test console, exercised extensively this session) already carried correct node ids without any seed change; this fix is purely about the *hand-authored fixture* runs being equally replayable.
- `[B5]` **Fixture `run_steps` rows now attribute `cost_cents`/`model`/token counts to the one `llm_call` step in each plan, using the parent run's own recorded totals — previously every seeded step left these `null`.** Only an LLM call ever costs anything for real (only `llmExecutor`, B4, returns `costCents`/token counts; every other executor doesn't), so this is a real attribution, not an invented one. Without it, the cost meter's real-time accumulation (gate item 6's whole point — "so you can see which node spent the money") would have shown $0 for the entire fixture-run replay and only jumped to the full total in the last instant, which is a poor demonstration of a feature the session's own Notes call the flagship of the build.
- `[B5]` **No genuine parallel/overlapping execution exists in this session's real data — B4's engine walks the topological order strictly one node at a time (see B4's own `runGraph` doc comment), so no real recorded run, seeded or live, ever has two steps with overlapping start/end windows.** Gate item 3 ("parallel node executions render as overlapping stacked segments") is therefore proven two ways deliberately short of a live concurrent run: `timeline-segments.test.ts`'s `computeSegmentLayout` is unit-tested directly against synthetic fixtures with real overlap (including a three-way overlap opening a third row), and `playhead.test.ts`'s "run B" fixture exercises `deriveNodeStatesAtTime` showing two nodes genuinely `"running"` at the same instant. Building real concurrent execution into the engine is a materially bigger change (per-branch scheduling, deterministic `stepIndex` ordering under concurrency) than this session's file list scopes, and B4's engine is already gated — not something to modify here.
- `[B5]` **`run-diff.tsx`'s comparison is keyed by graph node id, not `stepIndex`.** Two runs of the same agent can take different branches (a `condition` node's true/false path), so the same node can land at a different `stepIndex` in each run, or be entirely absent from one. `findChangedNodes` unions both runs' node ids and only calls a node "changed" if its formatted, successfully-produced output text actually differs — a node neither run reached, or both reached identically, is correctly not flagged. `computeLineDiff` is a small hand-rolled LCS backtrack (matching this codebase's own precedent for "basic," in-house text diffing over adding a dependency — see B4's condition-expression parser) since no diff library is in CLAUDE.md's stack table and the spec's own wording ("basic text diff is sufficient") doesn't ask for one.
- `[B5]` **"Raw provider request/response" (spec item 5's disclosure) shows the recorded resolved `input`/`output` verbatim, labeled honestly as such — not a fabricated raw HTTP transcript.** B4's `run_steps` schema never persisted the actual messages array, system prompt, or raw completion JSON a provider request/response would contain — only the resolved input, output, model, and token/cost summary (see B4's own executor design: `recordStep` only ever receives those fields). Inventing plausible-looking fake request/response JSON to fill the disclosure would be dishonest data; showing exactly what was recorded, captioned to say so, is the correct choice within this session's scope (extending `run_steps` to capture a real raw payload is a B4-schema change, out of bounds here).
- `[B5]` **Deep linking (gate item 6) is an explicit "Copy link to this moment" action, not a live-updating URL that rewrites browser history on every playhead tick.** `?t=<ms>` is read once, server-side, as the trace page's starting playhead — satisfying "deep link restores run and playhead position" by construction. Continuously syncing the URL to a 60Hz-capable playhead value (during a drag or playback) would mean up to 60 `history.replaceState` calls a second, which is unnecessary churn for a value that's only meaningful once someone deliberately wants to share it — matching how comparable products (Figma, Linear) expose "copy link to this moment" as a deliberate click rather than an ambient URL sync.
- `[B5]` **Run history's date filter is four coarse presets (All time/Today/Last 7 days/Last 30 days) via the existing `Select` primitive, not a from/to range picker.** No calendar/date-range primitive exists anywhere in this codebase's approved stack (`components/ui` has no Radix date picker, and CLAUDE.md names none), and building one is a real, unscoped primitive-design task this session's file list doesn't include. Status is a row of toggle buttons rather than a dropdown for the same reason A5's `DataTable` gate items favor direct manipulation over menus where the option count is small (four values) — every option is visible and each is natively `aria-pressed`-operable with no extra click to open a menu first.
- `[B5]` `e2e/agent-run-trace.spec.ts`'s run-diff test clicks the comparison-run `Select`'s trigger via `getByText(...)`, not `getByRole("combobox", { name })` — the latter hung indefinitely (60s timeout) despite `locator.ariaSnapshot()` confirming the exact same accessible name was computed correctly (`"Choose a run to compare against"`), and despite a plain `getByRole("combobox")` with no name filter resolving instantly. Root cause not fully isolated (a Playwright role-engine quirk specific to this trigger's `role="combobox"` + `aria-autocomplete="none"` combination inside a Radix Dialog portal, reproduced consistently across a half-dozen standalone repro scripts) — not an app bug: direct DOM inspection confirmed the element was visible, stable, and fully interactive throughout. Noted here since it's a trap a future session's test could burn significant time on again; `getByText` (or a `data-testid`) sidesteps it.

- `[B6]` **User decision: version history for the seeded "Recall Scheduler" agent was seeded by driving the real Publish dialog through Playwright, not by an ad-hoc script writing `agent_versions` rows directly.** First attempt was a direct-DB-write script; the user rejected that tool call and, when asked, chose "use the real UI instead." `e2e/agent-versions-publish.spec.ts`'s own first test (publishing twice with two different system prompts) is therefore both a real gate-item test *and* this session's seeding mechanism — the two real versions it creates are genuine production data, not fixtures, and every later test in that file is written to be idempotent against repeat runs leaving extra same-note versions behind (`.first()` throughout, never assuming exactly one match).
- `[B6]` **Immutability is enforced by a Postgres `BEFORE UPDATE` trigger on `agent_versions`, not just an application-layer check** — `prevent_published_agent_version_mutation()` (migration `0008_b6_immutability_trigger_update_only.sql`) raises if `OLD.published` is true. The trigger was originally `BEFORE UPDATE OR DELETE`, which is the more literal reading of "immutable," but that also intercepts a parent agent/workspace's cascade-delete (a `DELETE` on `agents` cascades to `agent_versions`, and the trigger fired on that cascade too, blocking it outright) — a real bug caught by this session's own cascade-delete test, not a hypothetical. Narrowed to `UPDATE`-only: a published version still can never be edited, but deleting its owning agent (which necessarily takes its whole version history with it) still works.
- `[B6]` **`lib/repos/agents.ts#listAgentsForIndex`'s `lastRunAt` needed an explicit `new Date(...)` the other three correlated-subquery columns didn't** — a real production 500 (`lastRunAt.toISOString is not a function`) confirmed live against `/w/meridian-ops/agents`, not caught by typecheck. `sql<Date | null>\`...\`` inside a Drizzle select is a compile-time-only annotation; postgres.js's automatic column-type parsing (which turns a real `timestamp` column into a JS `Date`) only applies to genuine column references, not an ad-hoc computed subquery — so the value comes back as a raw ISO string at runtime despite its declared type. `lib/repos/agents-index-perf.test.ts` asserts `lastRunAt instanceof Date` as a regression guard.
- `[B6]` **Draft isolation (gate item 2) is proven via a real in-flight run, not a synthetic delay.** `POST /api/agents/[id]/run` gained an optional `agentVersionId` — when present, the run reads that version's immutable `agent_versions.graph` snapshot instead of the live `agents.graph_jsonb` draft. The e2e test starts a run pinned to a real published version without awaiting it, edits the live draft to a drastically different, distinctive prompt while that run is still in flight, then confirms the run's real Groq output doesn't contain the new prompt's marker text. `agentVersionId` is deliberately not exposed anywhere in the builder UI — nothing in this session's scope calls for a "run this specific version" picker; it exists purely as the mechanism this gate item and any future scheduled/production run path would use.
- `[B6]` **Version diff (`features/agents/versions/diff.tsx`) always diffs a historical version against the *current live draft* — never version-against-version.** The spec's own panel action is worded "diff against current," and B6 built exactly that, not a version-picker-vs-version-picker comparison; `diffGraphs(base, compare)` takes `base` = the historical snapshot, `compare` = always the live draft passed down from the versions page.
- `[B6]` **Publish dialog's estimated cost per run (gate item 5) is a heuristic, not a real token count** — no run has executed yet at publish time, so there's nothing real to measure. It assumes 400 prompt / 150 completion tokens per LLM node in the draft and sums `estimateCostCents()` (B4's real per-model Groq pricing table) across every LLM node, rather than inventing a second pricing mechanism.
- `[B6]` **Only node-scoped validation issues (`ValidationIssue.nodeId` set) render as a clickable-to-focus button in the publish dialog — a graph-wide issue (`nodeId: null`, e.g. "no enabled trigger," "empty graph") renders as plain text.** There's no node on canvas to focus for a graph-wide failure, so gate item 5's "each failing node in the dialog is clickable to focus it" is read as applying to node-scoped issues specifically. `e2e/agent-versions-publish.spec.ts`'s gate-item-5 test breaks a real per-node config instead (the knowledge node's `topK` set to `0`, invalid against its `z.number().int().positive()` schema) rather than disabling the trigger, specifically so the failure it exercises is the clickable kind.
- `[B6]` **The docked Node Library panel (`top-left`, B1/B2) visually overlaps any node positioned near flow x:0 once `fitView` runs** — a real, pre-existing layout property of the canvas, not something this session introduced, but one this session's own e2e testing hit concretely for the first time (a click on the seeded Recall Scheduler's trigger node, at `x:0`, was silently intercepted by the panel's option list underneath it). Not fixed — redesigning canvas chrome is out of B6's scope — worked around at the test level by targeting a node further right instead. Left in Known issues below for whichever session next touches canvas layout.
- `[B6]` **Export/import legitimately produces two agents with the same name** — `e2e/agent-export-import.spec.ts` exports the real, published "Lumen Dental — Recall Scheduler" and re-imports it as a new draft agent, which correctly keeps the original's name (import doesn't invent a "(copy)" suffix the way Duplicate does, since round-tripping a name change would fail the "identical graph and config" gate item). `e2e/agents-index.spec.ts` disambiguates same-name rows by exact-text match on the name cell rather than assuming uniqueness.
- `[B6]` **`AgentsTable`'s row actions consolidated to a single kebab `DropdownMenu` (Duplicate, Export), not two adjacent icon buttons.** `DataTable`'s own `__actions` column (`components/patterns/data-table/data-table.tsx`) is a fixed 48px, sized for the one-affordance case B5's `RunsTable` established; two full-size icon buttons overflow it and the overflow is silently clipped by the cell's own `truncate` class — a real click-target bug (a click on the second button's apparent position was landing on the next cell over), not a cosmetic one.

## Deferred

Things a spec asked for that were consciously left undone, and why. Empty is the goal.

- _(none for A5 — every component, state, and story named in A5's scope is built)_
- `[A4]` Gate item 1's 60fps claim (12 concurrent Firing Bar segments) was verified structurally — `layout`-prop/transform-based resize, zero console/page errors across a headless run of every new story, `motion-firingbar--twelve-concurrent` story built and exercised — but the actual frame-rate number needs a human with the browser Performance panel open; that number can't be produced from this environment. Everything else in every A4 gate item was verified directly (typecheck, lint, a full Storybook build, and a headless Chromium pass over all 11 new stories with console/page-error capture plus an `axe-core` run showing zero violations); a real focus-ordering bug in the Firing Bar's hover-panel trigger (see decisions above) was caught and fixed this way, not left for someone else to find.
- `[A5]` Gate item 1's 60fps claim (50,000-row `DataTable` scroll) was verified structurally the same way A4's was — `@tanstack/react-virtual` only ever mounts visible+overscan rows (asserted directly in the `50,000 rows` story: DOM row count stays under 60 regardless of data length), dynamic row measurement via `measureElement` rather than a hardcoded height, zero console/page errors on a headless run. The actual frame-rate number needs a human with the browser Performance panel open.
- _(none for A6 — every scope item in the spec is built and gate-verified; see Decisions above for the one thing the spec didn't ask for that got built anyway — the Postgres role split — because RLS was silently a no-op without it)_
- `[B1]` Gate item 1's ≥55fps-at-p95 claim was verified with a real, automated frame-timing measurement (see Decisions above), not a human with the Chrome DevTools Performance panel open, which the gate's wording specifically calls for and this environment can't produce. Recorded anyway: p95 frame time 16.80ms → 59.5fps over ~600 sampled frames, 300 nodes / 400 edges, 10s of continuous pan+zoom. A human re-running `pnpm storybook` and manually profiling `Features/Agents/Canvas → 300 nodes / 400 edges` would give the authoritative number this gate item ultimately wants.
- `[B2]` Gate item 1's "drag-to-canvas" half of node-library insertion (as opposed to click-to-insert-at-center, which is e2e-verified) is implemented (`features/agents/library/index.tsx`'s `onDragStart` setting a custom MIME payload, read back by `features/agents/canvas/index.tsx`'s `onDrop`/`onDragOver`) but not exercised by an automated test — native HTML5 drag-and-drop (as opposed to React Flow's own mouse-based node/selection dragging, which Playwright's `page.mouse` API drives directly and reliably throughout this session's other tests) needs a real `DataTransfer` object and browser-native `dragstart`/`dragover`/`drop` events that Playwright's mouse API doesn't synthesize on its own. Verified by code inspection and manual reasoning about the two handlers' shared MIME-type contract, not by an automated drag gesture.
- `[B2]` The keyboard connection path's e2e coverage (`e2e/canvas-connections.spec.ts`) exercises Tab-equivalent node focus, Enter into port-focus, `c` to begin connecting, Esc to cancel, and `c` + Enter to complete a real connection — it does not separately assert that pressing an arrow key while connecting actually changes which candidate is targeted (it completes against the default first candidate). The arrow-handling code itself is a small, direct array-index increment (`features/agents/canvas/index.tsx`'s `"connecting"` phase branch) reviewed by hand rather than a class of logic likely to hide a subtle bug; a stronger e2e assertion would need a way to read which target an in-progress keyboard connection is currently pointing at from the DOM, which nothing in this session's scope exposes.
- `[B2]` Gate item 8's fps figure (same `e2e/canvas-benchmark.spec.ts` sampler B1 built) measured **2–4.6fps this session, far below the ≥55fps threshold** — but this is an environment ceiling hit *this session*, not a real regression from B2's node types, confirmed by a controlled experiment: swapping `NodeShell`'s real render (icons, borders, ports) for a single bare `<div>` with no ports produced an *equal-or-worse* number (2.07fps) in the same run, which rules out node visual complexity as the cause (a real paint-cost regression would have improved, not worsened, with less to paint). The other three benchmark assertions in the same test (pane visible, mounted-node count between 0 and 100, zero re-render on an isolated zoom-free pan) passed both before and after that experiment, on every run. Corroborating evidence the whole sandbox was under heavy load at the time, unrelated to app code: the *unit* test suite (`pnpm test`, no browser involved) took 38s on one run in the middle of this investigation versus its normal ~4–7s. A human re-running `pnpm storybook` and manually profiling `Features/Agents/Canvas → 300 nodes / 400 edges` — ideally on a quieter machine than this session had — would give the authoritative number this gate item wants; the structural aspects of gate item 8 (virtualization, zero re-render, real node types) are verified and passing.
- `[B3]` Gate item 2's "killing the network shows an error state with a retry" is verified two ways short of an actual scripted network-kill e2e: a Vitest test (`features/agents/lib/persist.test.ts`) drives `useAutosave` directly via `renderHook` with a `fetch` mock that rejects (and separately, one that resolves non-2xx), asserting the indicator reaches `"error"` and that calling `retry()` can recover to `"saved"`; and manual browser QA against the real app/API/Postgres (since deleted, scratch-only) confirmed the same `"error"`/retry UI renders correctly on a genuine failed `PATCH`. What's missing is a *committed* Playwright spec that kills the network mid-session in a live browser (e.g. via `page.route(..., route => route.abort())`) the way the gate's wording pictures — the Storybook-based `e2e/agent-inspector.spec.ts` mocks `fetch` at the module level for a different purpose (making the "saving" state observable at all) and wasn't extended to also simulate failure, for time reasons.
- `[B3]` Agent-level settings (`features/agents/inspector/agent-settings.tsx` — name/description editing when nothing is selected) has no automated test, Storybook or e2e. Its save-on-blur path was exercised only during manual QA (see the Decisions entry above) before being cleaned up. It's a small, low-risk component (two controlled inputs, one `fetch` on blur, no undo/coalescing) but a future session touching it should add coverage rather than assume this note still reflects reality.
- _(none for B4 — every gate item (1-8) was verified against the real, running app: a real Groq call end to end, `run_steps` rows read back from the DB, real streaming latency, real cancellation, real failure propagation, real cost accuracy, and a real browser confirming the console/canvas/FiringBar. See B4's Decisions above for how each was verified.)_
- `[B5]` Gate item 2's "stays at 60fps" claim was verified structurally, the same way B1's canvas pan/zoom claim was (see B1's own Deferred entry) — this environment has no interactive Chrome DevTools Performance panel for a human to read. What *is* verified directly: the architecture that makes 60fps possible (`replay-store.ts`'s granular per-node `nodeStates`/`activeEdgeIds` selectors, mirroring B1's canvas-store/graph-store physical split and B4's `run-store.ts` precedent — a node only re-renders when its own derived status changes, never on every one of the ~60 playhead writes a drag produces per second) is in place and exercised by a real mouse-drag e2e test (`e2e/agent-run-trace.spec.ts`'s "dragging the playhead moves it") that completes responsively against a real 6-node/6-step run. A human re-running `pnpm build && pnpm start` and manually profiling a drag across a longer (40-step) run's scrubber would give the authoritative number this gate item ultimately wants.
- _(none for B6 — every scope item is built and every gate item is verified against the real running app; see Decisions above for what was deliberately left out of the builder UI — an `agentVersionId` picker to run a specific published version — since nothing in the spec calls for one; it exists only as the mechanism gate item 2's test uses via a direct API call.)_

## Known issues

- _(none open — A6 resolved both prior entries: `pnpm test` now has real test files, and the semantic-contrast token gap is fixed. See A6's Decisions above for the contrast fix's specifics.)_
- Local dev-only: `pnpm dev`/`pnpm build` print "Found multiple lockfiles... Selecting C:\Users\shesh\package-lock.json" — a stray `package-lock.json` in the user's home directory (outside this repo), not something this project's tooling can fix. Harmless; Next still resolves modules from `pnpm-lock.yaml`/`node_modules` correctly.
- A6's Postgres setup needs one manual step outside `pnpm seed`/`pnpm db:migrate`: `psql "$DATABASE_MIGRATION_URL" -f db/setup-role.sql` once, to create the non-superuser `kiln_app` role RLS depends on (see A6's Decisions above for why). Not folded into `pnpm db:migrate` because role creation isn't a `drizzle-kit`-managed schema object — a fresh environment needs this run by hand once against whatever Postgres it's pointed at.
- `[B1]` Storybook serves `public/fonts/*.woff2` 404 on every story (not just the canvas's) — `.storybook/preview.tsx`'s `next/font/local` calls generate a `@font-face` URL that doesn't match where `staticDirs: ["../public"]` actually serves those files from. Pre-existing since A2/A4, unrelated to this session; found because `e2e/canvas-interactions.spec.ts`'s theme test is the first committed test in the repo that asserts zero console errors against a live Storybook page (that test's error filter explicitly excludes this one known message). Storybook still renders correctly with the fallback system font — cosmetic only, not something this session's scope covers fixing.
- `[B2]` This sandbox's long-running `pnpm storybook` process periodically fell into a broken state during this session — the page stuck reloading forever with `[HMR] Cannot find update (Full reload needed)` in the console and an empty `#storybook-root`, seemingly after several source edits landed in quick succession. A full process kill + restart (not just a page reload) reliably recovered it every time; letting Playwright's own `webServer` entry in `playwright.config.ts` own the process lifecycle end-to-end (rather than starting `pnpm storybook` by hand in the background and pointing ad-hoc test runs at it) avoided the problem entirely. Not something this session's scope covers fixing — root cause wasn't isolated, and every actual gate-verification run in this file was done against a freshly, cleanly started instance.
- ~~`[B3]` The same category of sandbox flakiness B2 hit with `pnpm storybook`...`PostgresError: invalid input syntax for type uuid: ""`...~~ **RESOLVED in B4 — this was never sandbox flakiness.** It was a real, deterministic Postgres/RLS bug (`current_setting(..., true)` returning `''` instead of `NULL` for a GUC a *previous* transaction on the same pooled connection had set locally), triggered specifically by several rapid single-key `withScope()` calls in one request — B3's own autosave/PATCH pattern (one `withScope` call per request) almost never triggered it, which is why it looked like rare, unrelated flakiness rather than a reliable repro. Fixed at the RLS-policy layer: `db/migrations/0005_fix_rls_empty_string_cast.sql`. See B4's Decisions above for the full mechanism and the minimal standalone repro that proved it. If this exact error resurfaces, it is **not** this issue recurring — that root cause is fixed — look for a *new* cause instead of assuming flakiness.
- `[B6]` The docked Node Library panel (top-left, `w-64`, nearly full canvas height — B1/B2) sits directly over any node positioned near flow x:0 once the canvas's initial `fitView` runs, silently intercepting clicks meant for that node (the panel's own option list is what actually receives the click). Confirmed concretely this session against the seeded Recall Scheduler's trigger node (`position: {x:0, y:120}`). Not a B6 regression and out of B6's scope to fix (it would mean redesigning canvas chrome, B1/B2's already-gated territory) — worth a future session's attention if a node ever needs to be reliably clickable at low flow-x coordinates, either by collapsing the panel, giving it an explicit toggle, or reserving a safe margin when auto-placing new nodes.
